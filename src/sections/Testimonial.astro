<!-- ---
import { Image } from 'astro:assets';
import Person1 from '../assets/person-1.jpg';
import Person2 from '../assets/person-2.jpg';

const testimonialData = [
  {
    name: 'Jessica M',
    rating: 5,
    handle: '@jessicam',
    image: Person1,
    description:
      'Working with Aaronn was an absolute pleasure! He took my vague ideas and transformed them into a stunning logo that perfectly represents my brand.',
  },

  {
    name: 'Liam K',
    rating: 5,
    handle: '@iamliamk',
    image: Person2,
    description:
      "I had been struggling to find a designer who could capture my brand's essence until I met Aaronn. His design was exactly what I was looking for.",
  },
];
---

<section class="pt-section">
  <div class="container">
    <h2>What People Are Saying</h2>

    <div class="content">
      {
        testimonialData.map((data) => (
          <div class="testimonial">
            <div class="rating" aria-label={`${data.rating} stars`}>
              {Array(data.rating)
                .fill(0)
                .map((_) => (
                  <i
                    class="fas fa-star"
                    style={{ color: 'var(--primary-clr-400)' }}
                  />
                ))}
            </div>

            <p class="description">{data.description}</p>

            <div class="person">
              <Image
                width={60}
                height={60}
                src={data.image}
                alt={data.name}
              />

              <div class="name">
                <h4>{data.name}</h4>
                <p>{data.handle}</p>
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  section {
    /* margin-bottom: 7rem; */ /* Using pt-section padding instead */
  }

  .content {
    background-color: var(--secondary-clr-800); /* Slightly lighter background */
    padding: 2.5rem; /* Increased padding */
    border-radius: 1rem;
    position: relative;
    /* Remove the ::after line separator for a cleaner look */
    /* content::after styles removed */
  }

  .rating {
    margin-bottom: 1rem;
    color: var(--primary-clr-400); /* Ensure stars use primary color */
  }

  .rating i {
    margin-right: 0.2rem; /* Adjusted spacing */
    font-size: 1.1rem; /* Slightly larger stars */
  }

  .testimonial:not(:last-child) {
    margin-bottom: 2rem; /* Adjusted spacing */
    padding-bottom: 2rem; /* Add padding to the bottom */
    border-bottom: 1px solid var(--secondary-clr-700); /* Add a subtle separator */
  }

   .testimonial:last-child {
     margin-bottom: 0; /* Remove bottom margin for the last testimonial */
   }

  .description {
    margin-bottom: 1.5rem; /* Adjusted margin */
    font-weight: 400; /* Slightly lighter font weight */
    font-size: 1rem; /* Adjusted font size */
    line-height: 1.7; /* Improved line height */
    color: var(--secondary-clr-200); /* Adjust color */
  }

  .person {
    display: flex;
    align-items: center;
    gap: 0.8rem; /* Adjusted gap */
  }

  .person img {
    --size: 50px; /* Slightly larger image size */
    width: var(--size);
    height: var(--size);
    object-fit: cover;
    border-radius: 50%;
    border: 2px solid var(--primary-clr-400); /* Add a border */
  }

  .name h4 {
    font-size: 1.1rem; /* Adjusted name font size */
    margin-bottom: 0.2rem; /* Adjusted margin */
    color: var(--secondary-clr-100); /* Name color */
  }

  .name p {
    color: var(--secondary-clr-300); /* Handle color */
    font-size: 0.9rem; /* Adjusted handle font size */
  }

  @media (min-width: 768px) {
    .content {
      display: grid;
      grid-template-columns: repeat(2, 1fr); /* Two columns on larger screens */
      gap: 3rem; /* Adjusted gap */
      padding: 3rem; /* Adjusted padding */
    }

    .testimonial:not(:last-child) {
      margin-bottom: 0; /* Remove bottom margin in grid */
      padding-bottom: 0; /* Remove bottom padding in grid */
      border-bottom: none; /* Remove border in grid */
    }

    /* Re-add a vertical separator for the grid layout */
    .content {
       border-right: 1px solid var(--secondary-clr-700); /* Add vertical separator */
    }
    .content > .testimonial:nth-child(odd) {
       border-right: 1px solid var(--secondary-clr-700); /* Add vertical separator for odd items */
       padding-right: 3rem; /* Add padding to the right of odd items */
    }
     .content > .testimonial:nth-child(even) {
       padding-left: 3rem; /* Add padding to the left of even items */
    }

     .testimonial {
       margin-bottom: 0; /* Ensure no bottom margin in grid */
     }
  }

   @media (min-width: 1024px) {
    .content {
      gap: 4rem; /* Increase gap on larger screens */
    }
     .content > .testimonial:nth-child(odd) {
       padding-right: 4rem; /* Adjust padding for larger gap */
    }
     .content > .testimonial:nth-child(even) {
       padding-left: 4rem; /* Adjust padding for larger gap */
    }
   }

</style> -->
